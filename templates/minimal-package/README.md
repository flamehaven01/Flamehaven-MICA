# example-project

<!-- MICA:INVOKE manifest="mica.yaml" -->

## MICA memory

Before working in this repository, run
`python tools/mica_runtime.py . --format context` and use the emitted archive
and playbook as session context. If the command refuses to emit, stop instead
of guessing.

# example-project Playbook

Replace this file with how work is actually done in this repository. The archive
says what has been decided; this says how to act on it.

## Before changing anything

- State which memory surfaces you loaded.
- Check the archive's design invariants. They are binding, not advisory.

## Verification

Replace with the command this repository is verified by, and what its output
means. A session should not have to guess how to prove its work.

```bash
# e.g. pytest -q
```

## When memory and reality disagree

The archive records what was decided, not what is currently true. If they
conflict, report the conflict rather than silently following either one.

<!-- MICA:INVOKE manifest="mica.yaml" -->
# Legacy Root-Manifest Consumer

This consumer keeps versioned memory filenames and a root manifest. MICA must
use the declared paths rather than rename them to match a newer template.

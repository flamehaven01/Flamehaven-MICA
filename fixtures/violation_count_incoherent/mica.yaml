mica_spec: "0.2.8"
name: violation-count-incoherent-fixture
mode: memory_injection
description: "Fixture: critical DI has violation_count > 0 but last_triggered empty. PCT-010 WARN (coherence). CLOSED CONTRACT."

layers:
  - name: archive
    path: memory/test.mica.v1.0.0.json
    format: json
    loading_hint: always
  - name: playbook
    path: memory/test-playbook.v1.0.0.md
    format: markdown
    loading_hint: always

invocation_protocol:
  primary_pattern: readme_protocol

update_triggers:
  - on_explicit_save

mica_spec: "0.2.5"
name: unbound-critical-di-fixture
mode: memory_injection
description: "Fixture: critical DI lacks binding. PCT-010 WARN. CLOSED CONTRACT preserved."

layers:
  - name: archive
    path: memory/test.mica.v1.0.0.json
    format: json
    loading_hint: always
  - name: playbook
    path: memory/test-playbook.v1.0.0.md
    format: markdown
    loading_hint: always

invocation_protocol:
  primary_pattern: readme_protocol

update_triggers:
  - on_explicit_save

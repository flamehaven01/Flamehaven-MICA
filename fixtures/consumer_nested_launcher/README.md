<!-- MICA:INVOKE manifest="memory/mica.yaml" -->
# Nested-Manifest Consumer With Launcher

Run `python tools/invoke_mica.py --format context`. The launcher delegates to a
released MICA runtime and does not duplicate invocation policy.
